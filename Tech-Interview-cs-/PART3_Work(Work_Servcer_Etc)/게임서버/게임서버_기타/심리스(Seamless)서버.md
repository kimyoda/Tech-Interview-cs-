# 심리스(Seamless) 서버란 무엇인가?

오늘 다른 신규팀 mmo rpg가 어떻게 구성이 되는지 궁금하여 물어봤었는데, 심리스서버라는 이야기를 하셨고, 어떤 서버인지 궁금하여 작성을 해봤다!

> 로딩 화면 없이 광활한 세계를 자유롭게 돌아다닐 수 있는 게임
> 심리스 서버 개념, 아키텍처 설계

---

## 1. 심리스 서버란?

전통적인 게임 서버는 맵이나 구역이 바뀔 때 "로딩 중..." 화면이 등장한다.
**심리스(Sameless) 서버**는 이 로딩 없이 플레이어가 끊김 없이 이동할 수 있도록 설계된 서버 구조이다.

### 기존 방식, 심리스 방식

| 구분        | 기존 서버 방식                    | 심리스 서버 양식                  |
| ----------- | --------------------------------- | --------------------------------- |
| 구역 이동   | 연결 끊김 -> 로딩 -> 새 서버 접속 | 투명하게 전환, 플레이어 인식 불가 |
| 구조 복잡도 | 단순                              | 복잡                              |
| 몰입감      | 로딩으로 몰입 저하                | 높은 몰입감 유지                  |
| 사례        | 일반 RPG 존 이동                  | WoW, EVE Online, GTA Online       |

> **대표 사례**: World of Warcraft(오픈 월드 대륙), EVE Online(솔라 시스템 전환), 엘든 링(멀티플레이 심리스), GTA Online(자유로운 이동)

---

## 2. 아키텍처 설계 - 존(Zone)기반 구조

심리시 서버의 핵심은 전체 맵을 여러 **존(Zone)** 으로 나누고, 각 존마다 독립적인 서버 프로세스를 두는 것이다.
존들 사이에 **경계 영역(Border Zone)**을 두어 부드러운 전환을 시도한다.

```
[ Zone A 서버 ] ── [경계 영역] ── [ Zone B 서버 ] ── [경계 영역] ── [ Zone C 서버 ]
     북부 대륙                        중부 평원                        남부 해안
         │                                │                                │
         └────────────────────────────────┴────────────────────────────────┘
                                          │
                                  [ World Manager ]
                             전역 상태 조율 · 플레이어 위치 관리
```

### 주요 구성 요소

- **Zone 서버**: 지정된 맵 구역의 게임 로직을 담당한다. 해당 구역의 플레이어, 몬스터, 오브젝트를 처리한다.
- **경계영역(Border Zone)**: 두 Zone이 겹치는 가상 공간이다. 이 구역에서 플레이어는 두 서버 모두에 동시 등록된다.
- **World Manager**: 전체 존의 상태를 조율하는 중앙 관리자. 어느 서버에 플레이어가 있는 지 추적한다.

### 설계 시 고려사항

- 존의 크기는 서버 한 대가 처리할 수 있는 적정 동시 접속자 수를 기준으로 결정한다.
- 경계 영역의 폭은 네트워크 레이턴시보다 충분히 넓게 서정해야 전환이 자연스럽다.
- World Manager가 단일 장애점이 되지 않도록 이중화 구성이 필요하다.

---

## 3. 존 간 캐릭터 이동 구현

캐릭터가 존 경계를 넘어가는 순간, 서버 입장에서는 꽤 복잡한 작업이 일어난다.

### 이동 처리 흐름 (핸드오프 프로세스)

1. **경계 영역 진입 감지**
   Zone A 서버가 플레이어의 좌표가 경계 영역에 들어왔음을 감지한다.

2. **Zone B 서버에 사전 등록**
   Zone A가 Zone B에 플레이어 정보(위치, 상태, 인베토리 등)를 미리 전달해 준비시킵니다.

3. **더블 등록 구간(Double Registration)**
   플레이어가 경계 영역 안에 있는 동안, A + B 두 서버 모두 해당 플레이어를 처리한다. 이 구간에서 두 서버는 플레이어 상태를 지속적으로 동기화한다.

4. **권한(Authority) 이전**
   플레이거가 경계를 완전히 넘으면, Zone B가 주 서버(Authority)가 된다. Zone A는 해당 플레이어의 데이터를 정리하고 연결을 해제한다.

5. **클라이언트 재연결(선택)**
   구현 방식에 따라 클라이언트를 Zone B 서버에 직접 재연결, 프록시/게이트웨이를 통해 투명하게 라우팅할 수 있다.

### 이동 중 주의할 점

- **패킷 중복/유실 방지**: 더블 등록 구간에서 A, B가 동시에 로직을 처리하면 충돌이 발생할 수 있다. 주 권한 서버(Authority)를 명확히 지정해야 한다.
- **이동 중 전투 처리**: 경계를 이동하는 순간 전투 중이라면 두 서버 중 어디서 판정을 내릴지 규칙을 명확히 정의해야 한다.

---

## 4. 서버 간 동기화 상태 및 관리

심리스 서버에서 어려운 것 중 하나는 **서버 간 상태 동기화**이다. 특히 경계 근처에 있는 플레이어들은 서로 다른 서버에 있어도 서로를 볼 수 있어야 한다.

### 동기화가 필요한 데이터

| 데이터 종류        | 설명                        | 동기화 빈도    |
| ------------------ | --------------------------- | -------------- |
| 플레이어 위치/방향 | 이동, 회전 정보             | 매우 높음      |
| 캐릭터 상태        | HP, 버프/디버프, 애니메이션 | 높음           |
| 인벤토리 / 아이템  | 줍기, 드롭, 거래            | 이벤트 발생 시 |
| 채팅 / 파티 정보   | 글로벌 채널, 파티 상태      | 필요 시        |

### 동기화 구현 방식

**1) 관심 영역(Interset Management/ AOI)**
모든 데이터를 모든 서버에 뿌리는 건 비효율적이다.
각 서버는 자신의 경계에 인접한 Zone의 데이터만 구독하다.
플레이어를 중심으로 일정 반경 내의 엔티티만 동기화 대상으로 삼습니다.

**2) 메시지 브로커 활용**
Zone 서버 간 직접 통신 대신, Kafka, Redis Pub/Sub 같은 메시지 브로커를 중간에 두면 결합도를 낮출 수 있다.

**3) 공유 데이터 스토어**
플레이어의 핵심 상태(레벨, 골드)는 Zone 서버가 직접 보유하지 않고, 공유 DB(Redis, 분산 캐시)에 두어 어느 서버든 최신 상태를 읽을 수 있게 한다.

### 상태 관리 원칙

> **단일 진실 공급원(Single Source of Truth)**
> 동일한 데이터는 반드시 한 곳에서만 쓰여야 한다.
> 여러 서버가 동시에 같은 데이터를 수정하면 충돌이 발생한다.

---

-> 심리스 서버 재정의 및 구체적 예시
심리스 서버는 로딩이 없는 서버가 아니다.

> 하나의 월드를 여러 서버가 나눠서 처리, 플레이어는 하나의 연속된 세계처럼 느끼게 만드는 구조

즉, **서버 책임 분산 + 상태 이관 + 연속성 보장**이다.

```
[플레이어가 느끼는 것]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  그냥 걸어서 이동했을 뿐 — 아무 일도 없었음
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[서버 내부에서 일어난 것]
  A 서버 처리 → 경계 감지 → B 서버 준비 →
  상태 이관 → 소유권 이전 → A 서버 정리
```

유저는 계속 같은 세계에 있다고 느낌, **실제 처리 주체**는 바뀐다.

---

### 2. 비심리스 구조와 차이

비심리스 구조(Zone 방식)

```
[마을 서버] ──로딩──▶ [사냥터 서버] ──로딩──▶ [던전 서버]
```

유저가 맵을 걸어서 이동:

- A 서버가 처리
- 경계 근처에 가면 B서버도 유저를 준비
- 어느 순간 A서버에서 B서버로 넘어간다

차이
| 구분 | 비심리스 | 심리스 |
|------|---------|--------|
| 서버 전환 | 명시적, 로딩 발생 | 투명하게 처리 |
| 경계 | 명확한 단절 | 완충 구간(Buffer Zone) |
| 상태 이관 | 저장 후 재로드 | 실시간 이관 |
| 구현 복잡도 | 낮음 | 높음 |
| 전환 중 이벤트 처리 | 불필요 | 반드시 고려 필요 |

---

## 게임 서버 개발자가 알아야할 개념들

아래의 개념으로 나눠서 이해하면 편하다

```
1. 월드 분할 -> 누가 어느 공간을 책임지는 지
2. 서버 책임 범위 -> Authoriative Server 개념
3. 관심 영역 관리 -> AOI(Area of Interest)
4. 상태 동기화 -> 실시간 상태 이관
5. 소유권 이전 -> Handoff 핵심
6. 전환 중 일관성 -> 어려운 부분
```

### 월드 분할

심리스 서버는 하나의 큰 월드를 여러 서버가 나눠서 관리한다.

**좌표 기반 분할 예시**

```
Zone A 서버 → x: 0    ~ 999
Zone B 서버 → x: 1000 ~ 1999
Zone C 서버 → x: 2000 ~ 2999
```

**지형 단위 분할 예시**

```
Zone A → 마을 동쪽 지역
Zone B → 중앙 평원
Zone C → 산악 지대
```

**어느 서버가 어떤 공간을 책임지는지**가 명확해얗 ㅏㄴ다. 구역 단위를 Zone, Cell, Region, Shard 같이 부른다.

---

### 서버 책임 범위 - Authoritative Server

각 서버는 자기 구역 안에 있는 것들만 책임진다.

> **Authoritative Server(권위 서버)**
> 최종 판정권을 가진 서버

플레이어가 A 지역에 있으면 그 플레이어의 실제 위치, 충돌, 전투 판정은 **A 서버가 최종 결정**한다.  
클라이언트가 "나 여기 갔어요"라고 보내도 진짜 인정할지는 A 서버가 결정한다.

심리스에서 이 **권한이 서버 간에 넘어간다**.
